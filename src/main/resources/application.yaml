spring:
  application:
    name: smart-parking-platform

  datasource:
    url: jdbc:postgresql://localhost:5432/smartparking
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true